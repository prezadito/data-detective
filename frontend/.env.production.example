# Production Frontend Environment Variables Template
# Copy this file to .env.production and fill in your actual production values
# WARNING: NEVER commit .env.production with real secrets to version control!

# ============================================================================
# API CONFIGURATION - REQUIRED
# ============================================================================

# Backend API URL (REQUIRED)
# This is where your FastAPI backend is hosted
# IMPORTANT: No trailing slash
#
# Examples:
# - Same domain with /api path: https://datadetective.academy/api
# - Subdomain: https://api.datadetective.academy
# - Different domain: https://api.example.com
VITE_API_URL=https://api.yourdomain.com

# ============================================================================
# APPLICATION CONFIGURATION
# ============================================================================

# Application name (displayed in browser tab, etc.)
VITE_APP_NAME=Data Detective Academy

# Application version (displayed in about page, footer, etc.)
VITE_APP_VERSION=1.0.0

# Environment name (production, staging, development)
VITE_ENV=production

# ============================================================================
# ANALYTICS - OPTIONAL
# ============================================================================

# Enable analytics tracking (true/false)
# This should match your backend ANALYTICS_ENABLED setting
VITE_ANALYTICS_ENABLED=false

# Analytics Provider: 'plausible' or 'google'
# Plausible is recommended for privacy-friendly, GDPR-compliant tracking
VITE_ANALYTICS_PROVIDER=plausible

# Your domain name (for analytics tracking)
# Example: datadetective.academy
VITE_ANALYTICS_DOMAIN=yourdomain.com

# Google Analytics 4 Measurement ID (only needed if using Google Analytics)
# Format: G-XXXXXXXXXX
# Get from: https://analytics.google.com/
VITE_ANALYTICS_ID=

# ============================================================================
# MONITORING - OPTIONAL
# ============================================================================

# Sentry Error Tracking DSN (optional but recommended for production)
# Sign up at: https://sentry.io/
# Format: https://abc123@o123456.ingest.sentry.io/123456
# NOTE: This should be a DIFFERENT DSN than your backend Sentry project
VITE_SENTRY_DSN=

# ============================================================================
# FEATURE FLAGS - OPTIONAL
# ============================================================================

# Enable experimental features (true/false)
# VITE_ENABLE_EXPERIMENTAL_FEATURES=false

# Enable debug logging in browser console (true/false)
# Set to false in production
# VITE_ENABLE_DEBUG_LOGGING=false

# ============================================================================
# DEPLOYMENT NOTES
# ============================================================================

# IMPORTANT: All Vite environment variables MUST be prefixed with VITE_
# to be exposed to the browser. Variables without VITE_ prefix will be ignored.

# 1. Set VITE_API_URL to your backend API URL
#    - Test it: curl https://your-api-url/health
#    - Should return: {"status": "healthy"}
#
# 2. Ensure backend ALLOWED_ORIGINS includes your frontend domain
#
# 3. Build the frontend with production config:
#    pnpm build
#
# 4. The built files will be in the dist/ directory
#
# 5. Deploy dist/ to your static hosting service:
#    - Netlify: netlify deploy --prod --dir=dist
#    - Vercel: vercel --prod
#    - Traditional server: Copy dist/ to /var/www/html/
#    - AWS S3: aws s3 sync dist/ s3://your-bucket/
#
# 6. Environment variables are baked into the build at build time
#    If you change .env.production, you must rebuild!
#
# 7. NEVER commit .env.production to version control!
#
# See docs/ENV_VARIABLES.md for complete documentation
